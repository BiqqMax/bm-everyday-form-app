export { createClient } from './browser'

export { createClient as getBrowserSupabaseClient } from './browser'
export { createClient as createBrowserSupabaseClient } from './browser'
export { createClient as createBrowserClientInstance } from './browser'

export { createClient as default } from './browser'
